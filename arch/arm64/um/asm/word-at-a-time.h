/* SPDX-License-Identifier: GPL-2.0 */
/*
 * word-at-a-time for the arm64 UML backend — the generic version
 * (UML guests have no MTE and use UML's own extable format, not the
 * native arm64 exception-table asm).
 */
#ifndef __UM_ARM64_WORD_AT_A_TIME_H
#define __UM_ARM64_WORD_AT_A_TIME_H

#include <asm-generic/word-at-a-time.h>

#endif
